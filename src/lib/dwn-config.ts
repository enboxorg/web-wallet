/**
 * Default DWN server endpoint and provider configuration.
 *
 * All hardcoded references to the Fly.io DWN server are centralized here so
 * that they can be changed in one place.
 */

/** Default DWN server endpoint used for agent initialization and new identities. */
export const DEFAULT_DWN_ENDPOINT = 'https://enbox-dwn.fly.dev';
